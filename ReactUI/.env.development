# Use Vite proxy (vite.config.ts) so browser calls same-origin /api/* instead of cross-origin CORS.
VITE_API_BASE_URL=

# Backend for SSR loaders (npm run dev:ssr)
SSR_API_TARGET=https://localhost:60927
