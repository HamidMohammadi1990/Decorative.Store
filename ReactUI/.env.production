# Backend URL for SSR loaders and prerender (npm run build:seo, npm run start:ssr).
# Must match the running Store.Api origin (see dotnet launch output).
SSR_API_TARGET=https://localhost:60927

# Public storefront URL for canonical links during prerender (optional for local builds).
# VITE_SITE_URL=https://dibagallery.com
